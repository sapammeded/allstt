name: Build FINAL ALLSTT APK

on:
  workflow_dispatch:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle 8.7
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.7'

      # USER-FRIENDLY BUILD ENGINE: normalize uploaded HTML modules before
      # anything is copied into the APK. Case-only duplicates are removed in
      # the temporary CI workspace, never from the user's repository history.
      - name: Normalize uploaded HTML modules
        shell: bash
        run: |
          set -euo pipefail
          python3 tools/normalize_html_modules.py

      - name: Apply HVSS native CENTRAL transport patch
        shell: bash
        run: |
          set -euo pipefail
          python3 tools/patch_hvss_native.py

      - name: Apply Android WebView JavaScript dialog and Java safety patch
        shell: bash
        run: |
          set -euo pipefail
          python3 tools/patch_android_js_dialogs.py

      - name: Apply STT Finding Notes hardening
        shell: bash
        run: |
          set -euo pipefail
          python3 tools/patch_stt_hardening.py

      - name: Build assets from uploaded HTML modules
        shell: bash
        run: |
          set -euo pipefail
          rm -rf app/src/main/assets
          python3 tools/build_allstt_assets.py
          test -s app/src/main/assets/launcher.html
          count=$(find app/src/main/assets -maxdepth 1 -type f -name '*.html' | wc -l)
          echo "HTML assets built: $count"
          test "$count" -ge 2
          find app/src/main/assets -maxdepth 1 -type f -name '*.html' -printf '%f\n' | sort

      - name: Preflight Java compilation
        shell: bash
        run: |
          set -euo pipefail
          gradle --no-daemon :app:compileDebugJavaWithJavac

      - name: Build APK from current repository
        shell: bash
        run: |
          set -euo pipefail
          gradle --no-daemon :app:assembleDebug

      - name: Verify APK
        shell: bash
        run: |
          set -euo pipefail
          test -s app/build/outputs/apk/debug/app-debug.apk
          unzip -t app/build/outputs/apk/debug/app-debug.apk >/dev/null

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: ALLSTT-FINAL-APK
          path: app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
